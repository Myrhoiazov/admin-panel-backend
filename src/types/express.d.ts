import { Request } from 'express';

// declare module 'express-serve-static-core' {
//     interface Request {
//         token?: string; // Добавляем свойство token (опциональное, если оно не всегда присутствует)
//         user?: {
//             id: number;
//             name: string;
//             email: string;
//             // Другие поля, которые есть в user
//         };
//     }
// }